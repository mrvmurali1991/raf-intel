fastapi==0.115.0
uvicorn==0.30.0
gunicorn==22.0.0
mysql-connector-python==9.0.0
google-genai==1.0.0
python-dotenv==1.0.0
simple-icd-10-cm==1.5.0
pydantic[email]==2.10.3
httpx==0.27.0
celery==5.4.0
redis==5.0.0
reportlab==4.2.0
# WeasyPrint: HTML/CSS -> PDF renderer used by app.services.radv.packet_builder
# for the RADV audit packet export. Picked over plain ReportLab because an
# HTML/Jinja template is dramatically easier to style and iterate on than
# programmatic flowables. Tradeoff: WeasyPrint needs native GObject/Pango/
# Cairo system libs at runtime (apt: libpango-1.0-0 libpangoft2-1.0-0,
# libcairo2). Unit tests mock the renderer so CI does not need the native
# stack; an integration skip-marker activates if WeasyPrint fails to import.
weasyprint==62.3
Jinja2==3.1.4
pypdf==4.3.1
hccinfhir==0.3.0
requests==2.32.3
argon2-cffi>=23.1.0  # Primary password hasher (argon2id)
passlib[bcrypt]==1.7.4  # Legacy: verify existing bcrypt hashes until fully migrated
bcrypt>=4.0,<4.1  # pin <4.1: passlib 1.7.4 reads bcrypt.__about__.__version__, removed in 4.1
PyJWT==2.8.0
pyotp==2.9.0
qrcode[pil]==7.4
slowapi==0.1.9
cryptography==44.0.0
defusedxml>=0.7.1
sentry-sdk[fastapi]==2.14.0
python-multipart==0.0.9
paramiko>=3.4.0
alembic==1.13.3
PyMySQL==1.1.1
openpyxl==3.1.5
pytest>=7.4.0
pytest-asyncio>=0.23.0
# SQLAlchemy==2.0.29  # No longer used — data_quality_monitor was the sole consumer and has been converted to mysql-connector-python. Safe to remove once confirmed no other references exist.
psutil==5.9.8
# --- Observability ---
opentelemetry-api>=1.24.0
opentelemetry-sdk>=1.24.0
opentelemetry-exporter-otlp-proto-grpc>=1.24.0
opentelemetry-instrumentation-fastapi>=0.45b0
opentelemetry-instrumentation-asgi>=0.45b0
opentelemetry-instrumentation-requests>=0.45b0
opentelemetry-instrumentation-celery>=0.45b0
opentelemetry-instrumentation-mysql>=0.45b0
opentelemetry-instrumentation-redis>=0.45b0
prometheus_client>=0.20.0
# --- Resilience ---
tenacity>=8.2
# --- ML: suspect-confidence calibration (services/raf/calibration) ---
# Fit step is a manual script (backend/scripts/fit_calibrators.py); runtime
# only does a joblib.load + sklearn predict_proba per suspect.
scikit-learn>=1.4
joblib>=1.3
numpy>=1.26
boto3>=1.34.0  # optional, only required when STORAGE_BACKEND=s3
