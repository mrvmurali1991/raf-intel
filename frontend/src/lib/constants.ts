/**
 * Shared application-wide constants.
 * Import from this file — never duplicate these values inline.
 */

/** CMS Medicare Advantage base payment per 1.0 RAF unit (CY2024 benchmark). */
export const MA_PAYMENT_PER_RAF = 11015.04;
