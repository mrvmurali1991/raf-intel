/**
 * /care-gaps — alias for /recapture (same content, different URL).
 *
 * Both URLs render the Recapture Gaps page so that external links
 * to /care-gaps continue to work alongside the sidebar entry at /recapture.
 *
 * Usage: nothing extra needed — Next.js renders this file at /care-gaps.
 */
export { default } from "@/app/recapture/page";
